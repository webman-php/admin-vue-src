export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // basic login path
  BASE_INSTALL = '/install',
  // basic home path
  BASE_HOME = '/database/table',
  // error page path
  ERROR_PAGE = '/exception',
  // error log page path
  ERROR_LOG_PAGE = '/error-log/list',
}
